{
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.2",
    "elm-test": "^0.19.1",
    "elm-verify-examples": "^3.1.0",
    "elm-xref": "^4.0.0"
  },
  "scripts": {
    "test": "elm-format --validate src/ && elm-verify-examples && elm-test && elm-xref"
  }
}
